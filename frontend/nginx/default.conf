server {
    listen 443 ssl;

    server_tokens off;
    server_name study-ai.ru;

    ssl_certificate /etc/ssl/certs/study-ai.crt;
    ssl_certificate_key /etc/ssl/certs/study-ai.key;

    location / {
            root /var/html/;
    }
}